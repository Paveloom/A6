Считывать значение левой границы промежутка? (l)
.true.

Считывать значение правой границы промежутка? (r)
.false.

Считывать значение параметра alpha_1?
.true.

Считывать значение параметра beta_1?
.true.

Считывать значение параметра gamma_1?
.true.

Считывать значение параметра alpha_2?
.true.

Считывать значение параметра beta_2?
.true.

Считывать значение параметра gamma_2?
.false.

Величина малости для входных параметров alpha_1, beta_1, alpha_2 и beta_2
1e-8

Показывать вывод при определении родов граничных условий?
.true.

Показывать вывод при итерациях метода стрельбы?
.true.

Первое начальное значение при вариации y(l) (только для метода стрельбы)
1.d0

Второе начальное значение при вариации y(l) (только для метода стрельбы)
5.d0

Первое начальное значение при вариации z(l) (только для метода стрельбы)
1.d0

Второе начальное значение при вариации z(l) (только для метода стрельбы)
5.d0

Какой метод решения использовать? (1 — метод стрельбы; 2 — метод прогонки)
1